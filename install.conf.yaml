- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work
    - ~/scripts/dev

- shell:
  - [git submodule update --init --recursive, Installing submodules]
